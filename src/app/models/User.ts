export interface User {
  firstName: string;
  lastName: string;
  email: string;
  // age?: number;
  // address?: {
  //   street?: string;
  //   city?: string;
  //   province?: string;
  // };
  isActive?: boolean;
  registered?: any;
  hide?: boolean;
}
